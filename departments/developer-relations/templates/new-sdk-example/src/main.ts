// BSV SDK example — minimal scaffold.
//
// Replace this with the concept your example is illustrating.
// Keep it small: one concept per example is a BSVA DevRel convention.

async function main(): Promise<void> {
  console.log("BSV SDK example — scaffold OK.");
  console.log("Edit src/main.ts to illustrate your concept.");
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
